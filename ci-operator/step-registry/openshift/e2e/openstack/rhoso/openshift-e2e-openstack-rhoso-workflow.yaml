workflow:
  as: openshift-e2e-openstack-rhoso
  steps:
    pre:
    - ref: openshift-e2e-openstack-rhoso-cluster-deprovision
    test:
    - ref: openshift-e2e-openstack-rhoso-cluster-provision
    - ref: openshift-e2e-openstack-rhoso-testing
    # post:
    # - ref: openshift-e2e-openstack-rhoso-clean # Is it going to be needed?

  documentation: |-
    The openshift-e2e-openstack-rhoso workflow executes a job for testing openshift on top of RHOSO.